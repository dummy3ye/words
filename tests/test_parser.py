import pytest
from src.core.parser import parse_logic

def test_alphabetical_sorting():
    words = ["zebra", "apple", "banana"]
    # This is a placeholder for your actual parsing logic once moved
    assert sorted(words) == ["apple", "banana", "zebra"]
